---
title: 'Enviar Mensagem de Modelo'
description: 'Enviar mensagem de modelo do WhatsApp para qualquer número de celular'
api: 'POST https://app.wizebot.com.br/api/v1/whatsapp/template/send'
contentType: 'application/json'
authentication: 'API Key'
---

## Parâmetros

| Parâmetro | Tipo | Obrigatório | Descrição |
|-----------|------|-------------|-----------|
| apiToken | String | Sim | Sua chave de API |
| phone_number_id | String | Sim | Número de telefone da conta do WhatsApp ID |
| template_name | String | Sim | Nome do modelo a ser enviado |
| phone_number | String | Sim | Deve começar com o código do país e apenas caracteres numéricos são permitidos |
| language | String | Não | Código do idioma do modelo (padrão: pt_BR) |
| components | Array | Não | Array de componentes do modelo com valores dinâmicos |

## Exemplo de Requisição

```bash
curl -X POST \
  'https://app.wizebot.com.br/api/v1/whatsapp/template/send' \
  -H 'Content-Type: application/json' \
  -d '{
    "apiToken": "API-KEY",
    "phone_number_id": "PHONE-NUMBER-ID",
    "template_name": "hello_world",
    "phone_number": "PHONE-NUMBER",
    "language": "pt_BR",
    "components": [
      {
        "type": "body",
        "parameters": [
          {
            "type": "text",
            "text": "John"
          }
        ]
      }
    ]
  }'
```

## Exemplo de Resposta

```json
{
  "status": "1",
  "message": "Template message sent successfully."
}
