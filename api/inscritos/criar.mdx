---
title: 'Criar Assinante'
description: 'Criar um novo assinante/contato'
api: 'POST https://app.wizebot.com.br/api/v1/whatsapp/subscriber/create'
contentType: 'application/json'
authentication: 'API Key'
---

## Parâmetros

| Parâmetro | Tipo | Obrigatório | Descrição |
|-----------|------|-------------|-----------|
| apiToken | String | Sim | Sua chave de API |
| phoneNumberID | String | Sim | Número de telefone da conta do WhatsApp ID |
| name | String | Sim | Nome do seu assinante |
| phoneNumber | String | Sim | Número de telefone do assinante com código do país (sem o sinal de +) |

## Exemplo de Requisição

```bash
curl -X POST \
  'https://app.wizebot.com.br/api/v1/whatsapp/subscriber/create' \
  -H 'Content-Type: application/json' \
  -d '{
    "apiToken": "API-KEY",
    "phoneNumberID": "PHONE-NUMBER-ID",
    "name": "NAME",
    "phoneNumber": "MOBILE"
  }'
```

## Exemplo de Resposta

### Sucesso

```json
{
  "status": "1",
  "message": "WhatsApp subscriber has been created."
}
```

### Erros Possíveis

<AccordionGroup>
  <Accordion title="Conta não encontrada" icon="xmark">
    ```json
    {
      "status": "0",
      "message": "WhatsApp account not found."
    }
    ```
  </Accordion>

  <Accordion title="Limite de assinantes excedido" icon="circle-exclamation">
    ```json
    {
      "status": "0",
      "message": "Subscriber limit has been exceeded. You cannot have more subscribers."
    }
    ```
  </Accordion>

  <Accordion title="Assinante já existe" icon="user">
    ```json
    {
      "status": "0",
      "message": "Something went wrong or subscriber already exist."
    }
    ```
  </Accordion>
</AccordionGroup>
