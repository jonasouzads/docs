---
title: 'Atribuir Chat à Equipe'
description: 'Atribuir o Chat de um Assinante a um Membro da Equipe'
api: 'POST https://app.wizebot.com.br/api/v1/whatsapp/subscriber/chat/assign-to-team-member'
contentType: 'application/json'
authentication: 'API Key'
---

## Parâmetros

| Parâmetro | Tipo | Obrigatório | Descrição |
|-----------|------|-------------|-----------|
| apiToken | String | Sim | Sua chave de API |
| phone_number_id | String | Sim | Seu número de telefone do WhatsApp |
| phone_number | String | Sim | Número de telefone do assinante |
| team_member_id | Integer | Sim | ID do Membro da Equipe |

## Exemplo de Requisição

```bash
curl -X POST \
  'https://app.wizebot.com.br/api/v1/whatsapp/subscriber/chat/assign-to-team-member' \
  -H 'Content-Type: application/json' \
  -d '{
    "apiToken": "API-KEY",
    "phone_number_id": "PHONE-NUMBER-ID",
    "phone_number": "PHONE-NUMBER",
    "team_member_id": "TEAM-MEMBER-ID"
  }'
```

## Exemplo de Resposta

```json
{
  "status": "1",
  "message": "Successfully Assign Agent to Conversation."
}
```

### Erros Possíveis

<AccordionGroup>
  <Accordion title="Assinante não encontrado" icon="xmark">
    ```json
    {
      "status": "0",
      "message": "Subscriber not found."
    }
    ```
  </Accordion>

  <Accordion title="Membro da equipe não encontrado" icon="user-xmark">
    ```json
    {
      "status": "0",
      "message": "Team member not found."
    }
    ```
  </Accordion>

  <Accordion title="Erro na atribuição" icon="circle-exclamation">
    ```json
    {
      "status": "0",
      "message": "Failed to assign chat."
    }
    ```
  </Accordion>
</AccordionGroup>
