---
title: 'Obter Assinante WhatsApp'
description: 'Obter Assinante Usando ID do Chat (Número de Telefone)'
api: 'POST https://app.wizebot.com.br/api/v1/whatsapp/subscriber/get'
---

# Obter Assinante

Esta API permite obter informações de um assinante específico usando seu número de telefone.

## Endpoint da API

<ParamField method="POST" type="string">
  Obter informações de um assinante específico via WhatsApp
</ParamField>

## Parâmetros

<ParamField query="apiToken" type="string" required>
  Sua chave de API
</ParamField>

<ParamField query="phone_number_id" type="string" required>
  Número de telefone da conta do WhatsApp ID
</ParamField>

<ParamField query="phone_number" type="string" required>
  Número de telefone do assinante
</ParamField>

## Exemplo de Solicitação

<CodeGroup>
```bash POST
curl -X POST \ 
'https://app.wizebot.com.br/api/v1/whatsapp/subscriber/get' \
-d 'apiToken=API-KEY' \
-d 'phone_number_id=PHONE-NUMBER-ID' \
-d 'phone_number=PHONE-NUMBER'
```
</CodeGroup>

## Exemplo de Resposta

<CodeGroup>
```json Sucesso
{
  "status": "1",
  "message": [
    {
      "subscriber_id": 144,
      "chat_id": "8801521455XXX",
      "first_name": "Md Ronok",
      "last_name": "Ronok",
      "email": "test@gmail.com",
      "gender": "Male",
      "label_names": "Label1,Label2"
    }
  ]
}
```

```json Erro
{
  "status": "0",
  "message": "Subscriber not found."
}
```
</CodeGroup>

## Teste a API

<Note>
  Para testar esta API, você precisará de sua chave API (apiToken) que pode ser encontrada no painel da WizeBot em Configurações > API.
</Note>

<Playground
  method="POST"
  endpoint="https://app.wizebot.com.br/api/v1/whatsapp/subscriber/get"
>
  ```json
  {
    "apiToken": "SUA_API_KEY_AQUI",
    "phone_number_id": "SEU_PHONE_NUMBER_ID",
    "phone_number": "5511999999999"
  }
  ```
</Playground>

<RequestExample>
  ```bash cURL
  curl -X POST \
    'https://app.wizebot.com.br/api/v1/whatsapp/subscriber/get' \
    -d 'apiToken=SUA_API_KEY_AQUI' \
    -d 'phone_number_id=SEU_PHONE_NUMBER_ID' \
    -d 'phone_number=5511999999999'
  ```

  ```javascript Node.js
  const axios = require('axios');

  const data = {
    apiToken: 'SUA_API_KEY_AQUI',
    phone_number_id: 'SEU_PHONE_NUMBER_ID',
    phone_number: '5511999999999'
  };

  axios.post('https://app.wizebot.com.br/api/v1/whatsapp/subscriber/get', data)
  .then(response => {
    console.log(response.data);
  })
  .catch(error => {
    console.error('Erro:', error);
  });
  ```

  ```php PHP
  <?php
  $url = 'https://app.wizebot.com.br/api/v1/whatsapp/subscriber/get';
  
  $data = array(
    'apiToken' => 'SUA_API_KEY_AQUI',
    'phone_number_id' => 'SEU_PHONE_NUMBER_ID',
    'phone_number' => '5511999999999'
  );
  
  $options = array(
    'http' => array(
      'header'  => "Content-type: application/x-www-form-urlencoded\r\n",
      'method'  => 'POST',
      'content' => http_build_query($data)
    )
  );
  
  $context  = stream_context_create($options);
  $result = file_get_contents($url, false, $context);
  
  echo $result;
  ?>
  ```
</RequestExample>

<ResponseExample>
  ```json Sucesso
  {
    "status": "1",
    "message": [
      {
        "subscriber_id": 144,
        "chat_id": "8801521455XXX",
        "first_name": "Md Ronok",
        "last_name": "Ronok",
        "email": "test@gmail.com",
        "gender": "Male",
        "label_names": "Label1,Label2"
      }
    ]
  }
  ```

  ```json Erro - API Key Inválida
  {
    "status": "0",
    "message": "Invalid API Key."
  }
  ```

  ```json Erro - Assinante Não Encontrado
  {
    "status": "0",
    "message": "Subscriber not found."
  }
  ```
</ResponseExample>
